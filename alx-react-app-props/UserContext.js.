// UserContext.js
// This file defines and exports the React Context for user data.
import React from 'react';

// React.createContext() creates a Context object.
// When React renders a component that subscribes to this Context object,
// it will read the current context value from the closest matching Provider above it in the tree.
// The `null` argument is the default value that will be used if a component
// consumes the context without a matching Provider above it.
const UserContext = React.createContext(null);

export default UserContext;
